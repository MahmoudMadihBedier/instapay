import 'package:flutter/material.dart';

class OfferSegement extends StatelessWidget {
  const OfferSegement({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(height: 160, width: 200, color: Colors.black),
    );
  }
}