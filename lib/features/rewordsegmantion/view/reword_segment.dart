import 'package:flutter/material.dart';

class RewordSegment extends StatelessWidget {
  const RewordSegment({super.key});

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(height: 160, width: 200, color: Colors.black),
    );
  }
}